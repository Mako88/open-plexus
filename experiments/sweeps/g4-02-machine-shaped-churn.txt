Churn shaped like a machine
===========================

Script: experiments/g4_02_machine_churn.py
Status: RUN on Actions, 18/18 cells. Predictions 1, 2 and 3 held.
        PREDICTION 4 REFUTED -- the gap SHRINKS with P rather than growing.
        And unlike g4-01, this grid contained its own answer.


IN PLAIN TERMS
--------------
When we tested what happens if machines drop out, we removed a random scattering
of the model's parts. That was the honest thing to do at the time, because there
were no machines -- the parts were not assigned to anybody.

Now they are. Each machine owns a contiguous block, and when a machine leaves it
takes its own block and nothing else. **A random scatter and a solid block are
not the same damage**, and only the scatter was ever measured.

The difference should matter. Under a scatter, every machine is left slightly
broken. Under a block, the machines that remain are perfectly intact -- there are
just fewer of them.


QUESTION
--------
G3 removed dimensions uniformly at random. In a partitioned deployment that is
the wrong shape: a departing machine takes its own contiguous block of the width,
which is exactly one group.

Compare, at the same fraction removed:

  SCATTERED  dimensions chosen uniformly at random   <- what G3 measured
  BLOCK      whole groups removed                    <- what actually happens

  width      64, 128
  P          1, 4, 8
  churn      0.0, 0.25, 0.5
  seq_len    96
  lr         0.02, 0.05, 0.1     <- swept on every arm
  seeds      1, 2, 3

Removal is permanent and happens after 8 epochs of training, then training
continues to 24 -- G3's protocol, so the numbers are comparable.

**BLOCK only runs where the churn fraction is a whole number of machines.** At
P=1 a machine is the entire model, so the only block levels are none and all;
P=1 therefore appears in the SCATTERED arm alone, as the tie back to G3. At P=4
and P=8 every level in the grid is exact.


PREDICTIONS -- written before the run
--------------------------------------
1. **The two arms leave the same width standing at every cell.** This is the
   connection control, and it is asserted inside the experiment rather than
   checked at aggregation, because a comparison where one arm removes more than
   the other is measuring size and calling it shape.

   *The original version of this prediction was different and it was wrong.* It
   said SCATTERED and BLOCK would agree at P=1 -- but at P=1 a machine IS the
   model, so block churn has no fractional setting. The first run of the control
   removed the whole model where scattered removed half, and would have reported
   block churn as catastrophic. **The control fired before the matrix was
   dispatched, which is the only reason it cost nothing.**

2. **At P>1, BLOCK beats SCATTERED.** Under BLOCK the surviving groups keep
   intact readouts, trained on dimensions they still have. Under SCATTERED every
   group is holed, and a group that has lost a third of its features has a
   readout fitted to features that no longer arrive.

3. **But not by as much as the story suggests**, because both lose the same
   number of KEY dimensions, and the key is shared -- every surviving group reads
   the full key regardless of which group owned the missing part. That is the
   correction this project made to note 009 after a test failed, and it puts a
   floor under BLOCK's advantage.

4. **The gap between BLOCK and SCATTERED grows with P.** More groups means each
   group is narrower, so a scattered hole takes a larger fraction of any one
   group's features.

Most likely to be wrong: (2). If BLOCK and SCATTERED come out equal at P>1, the
shared key dominates and *which* machine's dimensions went missing barely
matters -- which would be a cleaner and more robust story than the one predicted,
and therefore the favourable direction, and therefore the one to distrust.

**If BLOCK does not beat SCATTERED, check before believing:** that the readout
damage is actually being modelled -- `ablate` zeroes the readout columns as well
as the projections, so a group under SCATTERED does have a partly-zeroed readout,
and if it recovers instantly the ablation is not biting.


WHAT WOULD REFUTE WHAT
-----------------------
- surviving widths differ         -> the control fails and the run aborts; the
                                     arms are removing different amounts and
                                     nothing here can be read
- BLOCK beats SCATTERED, gap
  grows with P                    -> partitioning is a robustness gain as well as
                                     a C1 fix, and G3's number is pessimistic for
                                     a real deployment
- BLOCK equals SCATTERED          -> the shared key dominates the damage. G3's
                                     number transfers unchanged, and the churn
                                     story is simpler than predicted
- BLOCK is WORSE than SCATTERED   -> losing a whole voter costs more than holing
                                     every voter. Pooling is doing more work than
                                     the g4-01 framing assumes, and both notes
                                     need revising


WHAT THIS DOES NOT SETTLE
--------------------------
Churn during a sequence. Removal happens between sequences here, as in G3, so no
sequence is ever half-processed by a machine that vanishes mid-way. That is the
harder case and it is untouched.


RESULT
------
3 seeds, each arm at its own best lr. The equal-size control is asserted inside
the run, so all 18 jobs succeeding means both arms removed identical amounts
everywhere.

 width   P  churn  scattered    block   block-scat
    64   4   0.25      0.979    0.983       +0.003
    64   4    0.5      0.899    0.951       +0.052
    64   8   0.25      0.960    0.969       +0.010
    64   8    0.5      0.867    0.881       +0.014
   128   4   0.25      1.000    1.000       +0.000
   128   4    0.5      0.992    0.998       +0.006
   128   8   0.25      0.999    0.999       -0.001
   128   8    0.5      0.984    0.994       +0.010

P=1 runs scattered only: block churn has no fractional setting when the machine
is the whole model. Its numbers (0.999 / 0.997 / 0.967 at d=64) are the tie back
to G3.


PREDICTIONS 1, 2 AND 3 HELD
----------------------------
1. **Equal size.** Asserted in-run at every cell; no job aborted.

2. **BLOCK beats SCATTERED**, 7 of 8 cells positive and the eighth at -0.001.
   Mean +0.012.

3. **But not by much, because the shared key floors it.** Confirmed, and this is
   the prediction that turned out to matter most. Both arms lose the same number
   of KEY dimensions, and every surviving group reads the full key regardless of
   who owned the missing part. That is the correction this project made to note
   009 after a test failed, and it does exactly the work predicted: it puts a
   ceiling on how much the shape of the damage can matter.

**Where the effect actually lives is the worst case, not the mean.** The largest
gap, d=64 P=4 churn 0.5, per seed:

    scattered  [0.842, 0.917, 0.938]
    block      [0.940, 0.946, 0.967]

Block is not merely better on average -- it is far tighter. Scattered churn's bad
seed is 0.842 against block's 0.940. **A deployment cares about its worst machine
configuration, not its average one**, so reporting only the +0.052 mean would
understate this cell.


PREDICTION 4 REFUTED
---------------------
    P=4   mean gap +0.015 over 4 cells
    P=8   mean gap +0.008 over 4 cells

The gap **shrinks** with P. The prediction said it would grow, reasoning that
narrower groups mean a scattered hole takes a larger fraction of any one group.
That much is true and it is not the whole mechanism.

What it missed: at higher P the pooled answer is a larger ensemble, so it is more
robust to *both* damage shapes at once, which compresses the difference between
them. Meanwhile the shared-key damage -- identical in both arms by construction --
becomes a larger share of the total harm as groups get narrower. Both effects
push the gap down, and together they beat the one effect the prediction
considered.

**The reasoning was not wrong, it was partial**, which is the harder failure to
notice: a correct mechanism that is outvoted by mechanisms nobody listed.


THE PRE-REGISTERED CHECK, AND IT PASSES
-----------------------------------------
Prediction 2 coming out positive is the favourable direction -- partitioning being
a robustness gain as well as a C1 fix -- so this note required, in advance, a
check that scattered damage is genuinely biting rather than being shrugged off:

    d=64, P=4:  churn 0.0 -> 0.995,  churn 0.5 scattered -> 0.899

A persistent 0.096 loss after full retraining. The ablation bites. The check
passes.


AND THIS GRID CONTAINED ITS ANSWER
------------------------------------
g4-01's learning-rate grid pinned at an edge in all six of its rows, with the
interior value chosen zero times in twenty-four. Run through the same check
(`tools/grid.py`), g4-02 chose the interior value **11 times out of 25**.

So unlike g4-01's, these numbers are not carrying a tuning caveat. Worth stating
because the two sweeps used the identical grid -- the difference is the regime,
not the diligence, and that is precisely why the check has to be mechanical
rather than a habit.


WHAT THIS SETTLES
------------------
**G3's churn number transfers, and was mildly pessimistic.** Removing dimensions
at random is a slightly harsher test than machines leaving cleanly, by about
0.012 on average and rather more in the worst case. Nothing in G3 needs redoing.

**Shape matters less than the shared key.** The dominant term in churn damage is
that a departing machine takes part of a quantity everyone reads -- not that it
takes its own share. That is a fact about the row-split, and it is the same fact
that made a group better than a narrow model in g4-01: the key is shared, and
that cuts both ways.
