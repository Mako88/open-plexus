# Option record — select by the norm at the address

> **RECORD ONLY. This file carries no status.** Chosen, refused, untried or live-both lives
> in [DECISIONS.md](../../DECISIONS.md) alone. Here there are only events, and events do not
> un-happen, so nothing here can go stale. **Absence means untried.**
> Format and the CONFIG block: [README.md](README.md).

---

## What exists

- Nothing. It was built as one of two selection rules in decision 147 and did not survive
  the same day.

---

## What was tried, and what came back

### It collapses where plain addressing holds — `147`

    CONFIG  when    2026-07-29
            source  decision 147
            script  unrecorded
            task    families with exceptions
            model   two retrievals, one at the surface address and one at the concept
            knobs   selection by norm
            scale   unrecorded

    select by norm, exceptions    0.247
    plain addressing, exceptions  0.783

**Magnitude at an address says nothing about whether it is the right address.** The norm
grows with how much was written there, and how much was written there is a property of the
concept's popularity rather than of the query.

This is a criterion that cancels its own input, which is CLAUDE.md rule 7's shape: the
quantity used to decide moves with the thing it is meant to discriminate against.

### What replaced it, and why the replacement is different in kind — `148`

    CONFIG  when    2026-07-29
            source  decision 148
            script  unrecorded
            task    families with exceptions
            model   occupancy gate
            knobs   index_prefer inherit
            scale   3 seeds

Membership is *"is there anything here"*, not *"who has more"*. An unwritten address reads
**exactly 0.0**, so the threshold is structural rather than tuned, and there is no
magnitude to be fooled by. Record: [inherit-gate.md](inherit-gate.md).
