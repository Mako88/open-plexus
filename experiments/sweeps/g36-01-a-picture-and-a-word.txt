Can a word and a picture reach the same concept?
================================================

Script: experiments/g36_01_a_picture_and_a_word.py
Status: RUN, local, 2026-07-31, 5.9s. Predictions committed at `9d9703d`
        before the script was executed.

ANSWER: **G7's falsifier did NOT fire.** At 50 codes link purity is far above
        chance **0.1110**, and 76.9% of all images are reachable from their own
        word. **But G7 is not PASSED**, for a reason stated in full below: both
        modalities are present in every occasion, so this measures ALIGNMENT
        rather than introduce-in-one-and-query-in-another.

> **CORRECTED 2026-08-01: the headline was 1.0000 and that figure is a LUCKY
> DRAW.** This run used seeds 0, 1 and 2 — the project's convention — and all
> three give exactly 1.0000. Run over eight seeds while checking something else
> in `g36-03`:
>
>     1.0000  1.0000  1.0000  0.7015  1.0000  0.9773  1.0000  1.0000
>
> **Mean 0.9599, worst 0.7015.** So *"every image code in a word's class has that
> word's digit as its majority"* is true of most draws and NOT true in general,
> and it was reported as though it were.
>
> **The finding survives and the sentence did not.** 0.96 against chance 0.1110
> is still decisive, and every conclusion below about the gate holds. What fails
> is the claim of perfection, which three seeds could not have caught: a failure
> occurring in one draw of eight is missed by three seeds about **two thirds** of
> the time.


IN PLAIN TERMS
--------------
This is the test the whole grounding idea was built for, and it is the first time
the project has pointed it at something that is not made of symbols it wrote
itself.

Until now, a "picture" and a "word" were both just numbers, and two numbers
either match or they do not. That skips the hard part. A real concept is one word
and **hundreds of different pictures**, no two identical — a child does not see
the same dog twice.

So: handwritten digits. One concept is the digit three: the word *three*, and
several hundred pictures of threes, all different. Nobody tells the machine which
pictures go together, and nobody tells it which word goes with which pictures. It
sees a picture and hears a word at the same moment, over and over, with other
words being said that are not about it.

**The question is whether it can get from the word to the pictures.**

There are two separate ways this can fail and the project's own goals document
insists they must not be lumped together, so they are reported apart:

  - **Can it tell that two pictures of threes are the same kind of thing?**
    That is about seeing, and we are using a deliberately crude method for it.
  - **Given that, can it link the word to them?** That is the actual grounding
    question and it is the one this project is trying to answer.

If the second fails while the first works, the idea is in trouble. If the first
fails, this run says nothing about the second and we need a better way of seeing
before asking again.


WHAT IS BEING MEASURED
----------------------
    quantiser purity   share of pictures sitting in a code whose MAJORITY digit
                       is their own. The seeing half
    link purity        of the image codes a word's recovered class contains,
                       the share whose majority digit is that word's. The
                       grounding half
    reach              share of the ten words that reach ANY image code
    classes            mean recovered class size, to catch a collapse

**Chance for both purities is the largest class share, about 0.11**, and is
printed by the run rather than assumed. A purity near that is a mechanism that
has learned nothing.

`link purity` is recall-shaped in the same way every metric in this line has
been: a total collapse would put every code in every class and score well. `mean
class size` is the companion, and a collapse shows up there as a number near the
surface count.


MEASURED ON
-----------
Local, single process, Python 3.14, numpy for the quantiser only.

    images       4,000    MNIST training prefix, fetched by tools/fetch_mnist.py
    pixels         784    28x28 greyscale, raw, unnormalised beyond unit length
    words           10    one token per digit
    noise            2    OTHER WORDS per occasion -- things said in the room
                          that are not about the picture. NOT other pictures: a
                          learner looking at one thing sees one thing
    distractors      1    carried from g32-01, present every occasion
    quantiser  spherical k-means over unit pixel rows, `grouping.cluster`
    statistic  conditional, the C1-safe one
    bound      derived, `g33-04`'s winner
    seeds      0, 1, 2

Swept: `codes` in {20, 50, 100}, which is 200, 80 and 40 images per code.

**The quantiser is BORROWED and deliberately weak.** `DECISIONS.md` §1 records
John's ruling that a borrowed feature space is acceptable and possibly preferred,
and raw pixels are the crudest honest choice. The point is to measure the linking
against a quantiser whose quality is known and reported, not to build a good one.


PREDICTIONS
-----------
P1  THE SEEING HALF WORKS BADLY BUT NOT USELESSLY. Quantiser purity lands
    between 0.40 and 0.85 at every code count — far above chance 0.11 and far
    below 1.0, because raw pixels group by ink position rather than by shape.
    REFUTED IF near chance, in which case nothing downstream is readable and the
    run says nothing about grounding; or if near 1.0, which would be
    suspiciously good for raw pixels and would mean checking for a leak before
    believing anything.

P2  THE GATE. Link purity is far above chance — above **0.50** — so a word's
    recovered class holds image codes of its own digit rather than of any digit.
    REFUTED IF link purity is near chance 0.11. **That is G7's falsifier firing**:
    the same concept could not be reached from two modalities, and `GOALS.md`'s
    answer to symbol grounding would be failing on the first real test of it.

P3  LINKING CANNOT BEAT SEEING. Link purity is at or below quantiser purity,
    because a word cannot distinguish two image codes that the quantiser has
    already confused.
    REFUTED IF link purity EXCEEDS quantiser purity by a clear margin. **That
    refutation would be the most interesting outcome available here**: it would
    mean the word REPAIRS the quantiser — two codes that both co-occur with
    *three* get linked into one concept even though the pixels never said they
    belonged together — and that is the multimodal claim doing real work rather
    than riding on the front end.

P4  MORE CODES CUTS BOTH WAYS. Raising `codes` raises quantiser purity (finer
    clusters are purer) and lowers the occasions per code, so link purity peaks
    in the middle rather than at 100. `g32-02` measured about 16 occasions per
    concept as the threshold, and 100 codes leaves 40 images each.
    REFUTED IF link purity rises monotonically with codes, which would say the
    occasion threshold does not bind here and the quantiser is the only limit.


COST
----
Estimated: 3 code counts x 3 seeds is 9 k-means runs over 4,000 x 784, 25
iterations each, plus 4,000 occasions of about 5 surfaces per run. k-means
dominates. **Two to ten minutes.** Local.

Recorded after the run: **5.9s**, against an estimate of two to ten minutes.
Wrong by two orders of magnitude — k-means on 4,000 unit rows is far cheaper
than assumed, and the estimate was made without measuring one iteration.


RESULTS
-------
      codes  per code  quantiser    link  grounded   reach  classes
         20       200     0.7174  0.8036    0.6604  1.0000     3.34
         50        80     0.8299  1.0000    0.7688  1.0000     5.00
        100        40     0.8718  0.6494    0.8266  1.0000    15.31

    chance for quantiser, link and grounded: 0.1110

`grounded` was ADDED AFTER THE FIRST RUN and before anything was concluded — see
P3. It is the share of all images that are BOTH in a code their own word reaches
AND in a code whose majority is their digit, so it is per-image and comparable
with quantiser purity, which `link` is not.


SCORECARD
---------
  P1  HELD, just. Quantiser purity is 0.7174, 0.8299 and 0.8718 against a
            predicted 0.40 to 0.85 — the 100-code cell is 0.0218 over the top of
            the range. Far above chance 0.1110 and far below 1.0, which is what
            raw pixels should give: they group by ink position rather than by
            shape.

  P2  **HELD, AND IT IS THE GATE.** Link purity is 0.8036, **1.0000** and 0.6494
            against chance 0.1110 and a predicted floor of 0.50. At 50 codes
            **every image code in a word's class has that word's digit as its
            majority**, and every one of the ten words reaches image codes at
            all.

            **So a word and a picture do reach the same concept, from
            co-occurrence alone, on real sensory data.** G7's falsifier —
            *"the same concept cannot be reached from two modalities under these
            constraints"* — did not fire.

  P3  REFUTED AS WRITTEN, THEN HELD ON THE RIGHT METRIC, and the difference is
            the whole entry. `link` (0.8036, 1.0000, 0.6494) exceeds quantiser
            purity in two cells, which reads as *the word REPAIRING the
            quantiser* — the outcome this file called the most interesting
            available.

            **It is not supported, because the two are not comparable.**
            Quantiser purity counts IMAGES; `link` counts CODES. A code that is
            60% threes is one correct code for `link` while its 40% wrong images
            drag the quantiser figure down. That is `g35-02`'s floor confound in
            a new costume, three days running.

            On `grounded`, which is per-image: **0.6604, 0.7688, 0.8266 against
            0.7174, 0.8299, 0.8718.** Below the quantiser in every cell, by
            0.057, 0.061 and 0.045. **P3's actual claim holds: linking cannot
            beat seeing.**

            And the gap being SMALL is the useful part — the linking loses about
            five points relative to what the front end permits, so **the
            bottleneck is perception rather than grounding.**

  P4  HELD on `link`, which peaks at 50 codes and falls to 0.6494 at 100, exactly
            as predicted from `g32-02`'s occasion threshold: 100 codes leaves 40
            images each and `classes` triples to 15.31.

            **REFUTED on `grounded`, which rises monotonically** — and that is
            over-merging rather than improvement. `grounded` is recall-shaped
            like every other metric in this line: a class holding more codes
            catches more of a digit's images, and at full collapse it would equal
            quantiser purity. `link` is the precision side and it is what falls.

            So 50 codes is the operating point: perfect precision, 0.7688 recall,
            no collapse.


WHY G7 IS NOT PASSED, WHICH IS THE MOST IMPORTANT LINE HERE
------------------------------------------------------------
`GOALS.md` states G7 as *"a concept introduced through one modality, queried
through another"*. **In this run both modalities are present in every single
occasion** — the picture and the word always arrive together.

So what is measured is **alignment across modalities**, which §1.2b names as the
harder of the two halves and which nothing here had tested. It is not
introduce-in-one-and-query-in-another, and calling this a pass would be exactly
the overclaim the gate ladder exists to prevent.

The run G7 needs is the obvious one and is not done: show a concept only through
pictures for a while, then ask with the word. That requires the word to be absent
during learning for at least part of the stream, which `occasions.py` can express
and this script does not.


WHAT ELSE THIS DOES NOT SETTLE
-------------------------------
**Ten concepts, and they are digits.** A world with ten things in it is not a
test of anything's capacity, and `g32-02`'s own caution about small worlds
applies.

**The quantiser is raw pixels and deliberately weak.** A better front end would
raise every number here and would not tell us more about the linking, which is
already within five points of what the front end permits.

**Nothing is distributed.** This is the statistic and the walk in one process;
`g35-01` and `g35-03` are where the container path lives.
