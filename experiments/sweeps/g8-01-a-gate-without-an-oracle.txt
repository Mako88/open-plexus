Can any real mechanism replace the oracle gate?
===============================================

Script: experiments/g8_01_real_gate.py
Status: RUN on Actions, 36/36 cells. Predictions 1, 3 and 4 HELD;
        2 is UNRESOLVED. **No mechanism tried recovers the oracle.** The
        largest recovery anywhere in the grid is 0.05 and most cells are
        at or below zero. Every result that rests on the gate is a CEILING.
        Predictions were written before the script existed, before the
        controls ran, and before any number from this grid was seen.


WHY THIS IS THE ONE THAT MATTERS
---------------------------------
Three of the project's best findings run through a gate that reads the answer
key. g7-02 (length stops mattering), g7-03 (allocation stops mattering) and the
tiny-node result itself all depend on `run(store=mask)`, and that mask comes from
`position_kinds()` -- the task telling the model which positions are worth
storing. **It is a ceiling, not a mechanism**, and note 011 named the gap between
it and plain decay as the honest size of what remains unsolved.

The reason it is load-bearing rather than incidental is one equation. Retrieval
quality goes as `SNR = sqrt(d/N)` -- node width over things stored -- measured
within 5% across a 16x range. Ungated, every consecutive pair is written, so `N`
is the sequence length and holding quality fixed as sequences grow means growing
`d` with them. **That is what kills tiny nodes.** Gated, `N` is twice the number
of real bindings whatever the length, which is why the four rows of g7-02 were
identical to three decimals.

So the tiny-node thesis and the gating problem are the same problem, and this
sweep asks the only question that settles it: **how much of the oracle's
advantage can a mechanism that reads nothing it should not read actually
recover?**


THE ARMS
--------
All four are the same model on the same data, differing only in what gets
written into the lasting store.

  none          store every consecutive pair, fade by decay      the FLOOR
  oracle        store only real bindings, read from the task     the CEILING
  on-use        promote a retrieval that was confirmed correct   real
  salience      promote a step whose surprise is unusual         real

`on-use` is tagging and capture (note 010, from Lehr et al. 2022): tag now,
cheaply, and let a LATER signal decide what survives. It needs a second occasion
to be confirmed on, which plain MQAR never provides because each binding is asked
about exactly once, at the end. **Recurrent MQAR is what makes this arm
testable at all** -- `queries_per_pair=3`, already built and pinned
bit-identical to the old generator by a golden digest.

`salience` fires on the tails of its own surprise distribution. Note 013 measured
it as genuinely selective -- query positions fire at 7.6x the filler rate -- and
still losing, because filler is 92% of the sequence and a 7.6x enrichment does
not survive that base rate.


THE QUESTION IS A RATIO, NOT AN ACCURACY
-----------------------------------------
The reported quantity is **recovery**: (arm - none) / (oracle - none), per
sequence length. 0 means the mechanism bought nothing the floor did not already
have; 1 means it matched a gate that cheats.

Reporting raw accuracy would hide the thing being asked, because all four arms
rise and fall together with length and learning rate. **A caveat next to the
number does not attach to the number**, so the ratio is what the summariser
prints and the raw accuracies go beside it, not the other way round.

Where `oracle - none` is smaller than the seed spread, recovery is undefined and
must be printed as undefined rather than as a large number with a note. That is
the failure mode g5-01 produced twice.


PREDICTIONS -- written before the run, before the script, before the controls
-----------------------------------------------------------------------------
1. **Both real arms recover less than half the oracle's advantage at every
   length.** Everything measured so far points here: consolidate-on-use lost to
   plain decay in g7-04, and salience lost at every threshold in note 013. Guess:
   recovery under 0.5 in all cells, and under 0.25 for salience.

   This is the unfavourable direction and it is the one the evidence supports, so
   it is stated first and plainly.

2. **on-use beats salience at every length.** Confirmation is a signal about
   whether a binding was USEFUL; surprise is a signal about whether a token was
   PREDICTABLE, and note 013 established those are not the same thing on this
   task. Recurrent MQAR gives on-use the second occasion it was missing and gives
   salience nothing new.

3. **Recovery FALLS as sequence length grows.** The oracle's advantage comes from
   holding `N` constant, and the longer the sequence the more filler a real
   mechanism has to reject to keep up. A fixed per-step selectivity cannot hold a
   constant `N` against a growing sequence -- it can only hold a constant
   FRACTION. Guess: recovery at 768 is below half of recovery at 192.

   **This is the prediction that would hurt most if it holds**, because it says
   the gap widens exactly where the gate is most needed.

4. **Control, expected to hold trivially:** `oracle` beats `none` by a margin
   larger than the seed spread at every length tested. If this fails the sweep is
   vacuous -- there is no advantage to recover -- and nothing else in the table
   means anything.

Most likely wrong: (2). If salience wins, then usefulness is not the right signal
and the tagging-and-capture reading of note 010 is wrong about this system.


WHAT WOULD REFUTE WHAT
-----------------------
- recovery near 1 for either arm     -> the oracle is replaceable and the tiny
                                        node results survive intact. The best
                                        outcome available and the least likely
- recovery 0.5-0.9                   -> partial. The gate is real but lossy, and
                                        the tiny-node claim needs restating with
                                        a measured penalty attached
- recovery near 0 for both           -> selective storage is not reachable by any
                                        local rule tried, and every result that
                                        depends on the gate must be re-labelled a
                                        CEILING in GOALS, not a finding
- recovery falls with length (3)     -> worse than a constant loss: the mechanism
                                        fails where it is needed most
- oracle does not beat none (4)      -> vacuous grid, discard and redesign


WHAT THIS CANNOT SETTLE
------------------------
**Whether a better mechanism exists.** Two candidates lose does not mean the
class is empty, and a negative result here bounds what has been tried rather than
what is possible. The honest statement in that case is that no local rule tried
so far recovers the oracle -- not that none can.

**Anything about real data.** MQAR's filler is uniformly random, which note 013
identified as the specific reason salience drowns: the uninformative class is 92%
of the sequence and maximally unpredictable. Real language is the opposite -- the
rare token is both surprising AND informative. **A negative result here is
therefore weak evidence about language and strong evidence about MQAR**, and must
be written up that way.


RESULT
------
Recovery, `(arm - none) / (oracle - none)`, at each cell's best learning rate.
Three seeds. The seed spread is printed beside the oracle's advantage so the
denominator can be checked against the noise rather than assumed.

 seq_len  half-life  oracle gap  seed spread    on-use  salience
     192        0.5       0.041        0.019     -0.02     -0.02
     192       0.25       0.081        0.022      0.00     -0.01
     192      0.125       0.212        0.022      0.05      0.04
     384        0.5       0.092        0.045     -0.03     -0.03
     384       0.25       0.137        0.035      0.01      0.01
     384      0.125       0.275        0.054      0.04      0.04
     768        0.5       0.612        0.152     -0.05     -0.05
     768       0.25       0.536        0.137     -0.02     -0.05
     768      0.125       0.538        0.097     -0.00     -0.01
    1536        0.5       0.996        0.004     -0.00     -0.00
    1536       0.25       0.985        0.012     -0.00     -0.00
    1536      0.125       0.959        0.012     -0.01     -0.01

**The largest recovery in the entire grid is 0.05.** Seven of twelve cells are
negative -- the mechanism is worse than not having it. Nothing here is
distinguishable from a gate that never fires.

Meanwhile the oracle scores 0.998 to 1.000 in EVERY cell, at every length and
every forgetting rate, while the ungated floor falls from 0.385 at seq 192 to
0.000 at 1536. That is g7-02 reproduced on a different grid: selective storage
removes sequence length as a difficulty dial, completely.

So the advantage is real, it is enormous -- up to 0.996 -- and none of it is
reachable by anything implementable that has been tried.


THE PREDICTIONS
----------------
1. **Both real arms recover less than half, salience under 0.25 -- HELD**, and
   by a margin that makes the guess look timid. Nothing exceeded 0.05.

2. **on-use beats salience at every length -- UNRESOLVED.** on-use is ahead or
   level in ten of twelve cells, but by 0.00 to 0.03 against seed spreads of
   0.004 to 0.152. **The differences are inside the noise and this is not a
   result.** It was named before the run as most likely wrong; it is not wrong,
   it is unmeasurable at this grid's resolution, which is a different outcome
   and is recorded as one.

3. **Recovery falls with sequence length -- HELD.** The best cell at seq 192 is
   0.05; at 1536 the best is -0.00. The gap widens exactly where the gate is
   most needed, which is the outcome named in advance as the one that would hurt
   most.

4. **Control: the oracle beats none by more than the seed spread -- HELD in all
   twelve cells**, so no row is undefined and the grid is not vacuous.

   **The pre-dispatch control was wrong about this**, and in the favourable
   direction. It predicted seq 192 would be undefined because a single-seed
   probe put the gap at 0.043 against an unmeasured spread. With three seeds the
   spread is 0.019 and the gap 0.041, so the row is defined after all. A control
   run at one seed cannot estimate a spread, which is the whole reason the
   summariser refuses to divide rather than guessing.


WHAT THIS SETTLES, AND IT IS THE UNFAVOURABLE ANSWER
------------------------------------------------------
**Selective storage is not reachable by any local rule tried here.** Consolidate
on confirmed use and consolidate on surprise both fail, at every length, every
forgetting rate and every learning rate in the grid, and they fail by recovering
nothing rather than by recovering a little.

Which means, plainly: **g7-02, g7-03 and the tiny-node result are ceilings, not
findings.** They describe what a device could do if something told it which of
its inputs mattered. Nothing tried can tell it.

That has to be written into GOALS as a relabelling, not a footnote.


WHAT IT DOES NOT SETTLE
------------------------
**Whether a better mechanism exists.** Two candidates failing does not empty the
class. The honest statement is that no local rule tried so far recovers the
oracle -- not that none can. Two specific untried candidates are recorded in
BACKLOG.md, and one of them is now the most interesting thing in the project:
**replay**, an offline phase that revisits stored traces later. The gating
problem is that the storage decision must be made at the moment the least is
known; every mechanism tried here makes that decision at exactly that moment.

**Whether MQAR is the reason.** Note 013 diagnosed the failure as a base-rate
problem: filler is 92% of the sequence and, being uniform, is also the most
surprising content in it. **That diagnosis is untested and g8-02 tests it** --
same arms, same metric, Zipfian filler. If recovery moves there, this result is
about the benchmark rather than about gating, and this file will need a header
saying so.


CORRECTION -- THE SEQ-1536 ROW IS WITHDRAWN
=============================================
Found while auditing assumptions after note 018, and it invalidates the most
quoted numbers in this file.

**Every one of the nine cells at seq_len 1536 has the `none` arm BELOW the
trivial floor of 0.34375.** Per seed:

    half   lr      none per seed        mean    above floor?
     0.5  0.02  0.247/0.199/0.164      0.203       NO
     0.5  0.05  0.051/0.051/0.021      0.041       NO
     0.5   0.1  0.004/0.004/0.000      0.003       NO
    0.25  0.02  0.316/0.255/0.242      0.271       NO
    0.25  0.05  0.086/0.083/0.074      0.081       NO
    0.25   0.1  0.010/0.022/0.010      0.014       NO
   0.125  0.02  0.385/0.304/0.292      0.327       NO
   0.125  0.05  0.182/0.143/0.151      0.159       NO
   0.125   0.1  0.034/0.042/0.044      0.040       NO

seq 192, 384 and 768 are all fine -- 9 of 9 cells above the floor at each.

So at 1536 the recovery denominator, `oracle - none`, is the distance between a
working ceiling and a **broken floor**. That is not an advantage a mechanism
could recover; it is the gap between something and nothing. **g7-04 caught
exactly this and this project wrote it down** -- "its largest margin, +0.249 at
1536, is between two failures, both at/below the 0.344 floor". g8-01 repeated it
one sweep later.

**And the tool preferred those cells.** The summariser selected the learning rate
that MAXIMISES `oracle - none`, and a broken floor arm is what maximises it. So
the reporting rule actively sought out the cells where the floor had failed: at
1536 it chose lr 0.1, where `none` is 0.003.

Corrected table, with cells whose floor arm is below the trivial floor excluded:

 seq_len  half-life  oracle gap  seed spread    on-use  salience
     192        0.5       0.041        0.019     -0.02     -0.02
     192       0.25       0.081        0.022      0.00     -0.01
     192      0.125       0.212        0.022      0.05      0.04
     384        0.5       0.092        0.045     -0.03     -0.03
     384       0.25       0.137        0.035      0.01      0.01
     384      0.125       0.275        0.054      0.04      0.04
     768        0.5       0.612        0.152     -0.05     -0.05
     768       0.25       0.536        0.137     -0.02     -0.05
     768      0.125       0.538        0.097     -0.00     -0.01

WHAT SURVIVES. The headline is unchanged: **no mechanism recovers the oracle.**
The largest recovery over the usable range is 0.05 and most cells are at or below
zero, exactly as before.

**Prediction 3 also survives, with a shorter reach.** Recovery still falls with
length at every half-life across 192 to 768 -- 0.05 / 0.04 / -0.00 at half-life
0.125, and similar declines at 0.25 and 0.5. What is withdrawn is the ENDPOINT.
The decline runs to seq 768, not to 1536.

WHAT IS WITHDRAWN.
- "the ungated floor falls to 0.000 at 1536" -- true and meaningless; the model
  is not doing the task there at all
- "the gap reaches 0.996" -- withdrawn. The largest gap over a usable range is
  0.612
- "recovery 0.05 at 192 to -0.00 at 1536" -- restate as 0.05 at 192 to -0.00 at
  768

Note 015's argument was built on the withdrawn phrasing. Its direction is intact
-- recovery does fall with length -- but its most dramatic evidence is not.


THE BAR A CANDIDATE MUST CLEAR, DERIVED 2026-07-30
=====================================================
**Added long after the run, and derived rather than measured.** Nothing was
re-run; this is arithmetic over two numbers this file and note 013 already carry,
written down because "no mechanism tried recovers the oracle" does not tell the
next person what WOULD.

This sweep's diagnosis is that `salience` is genuinely selective -- query
positions fire at **7.6x the filler rate** -- and still loses **because filler is
92% of the sequence and a 7.6x enrichment does not survive that base rate**.

What that enrichment actually buys, and what would be needed:

    stored set is % real     enrichment required
                     50%                   11.5x
                     75%                   34.5x
                     90%                  103.5x
                     95%                  218.5x

    measured best (salience)               7.6x
    the stored set 7.6x buys              39.8% real

**So the best mechanism ever measured here does not reach the HALFWAY bar.** At
7.6x, six of every ten things stored are still filler, `N` stays
filler-dominated, and `SNR = sqrt(d/N)` barely moves -- which is exactly the null
this sweep found, now with a mechanism rather than a shrug.

**Why writing it down is worth more than another sweep.** A candidate gate can now
be rejected ON PAPER: measure its enrichment on a stream, compare against 11.5x,
and only build it if it clears. Four mechanisms have been measured and failed
(`salience` and `on-use` here, `capture_slots` in g8-03, a fast-store cap in
g8-04) and none of them was screened this way first.

**And it reframes the target.** The gap is not "a bit better than 7.6x". It is an
ORDER OF MAGNITUDE, and a signal that strong is unlikely to be a threshold on a
scalar -- which is a reason to look at structural signals (does this address
already exist, does this token recur) rather than at better-tuned magnitudes.
Note 010's objection stands against the obvious version of that: in MQAR the only
event separating a pair from filler is the query, which arrives too late and never
recurs. **So the instrument may have to change before the mechanism can.**
